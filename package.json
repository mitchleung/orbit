{
  "name": "orbit",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "sass-dev": "sass --watch --update --style=expanded scss:css",
    "sass-prod": "sass --no-source-map --style=compressed scss:css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dev@mitchleung.com",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.50.0"
  }
}
